#lang racket
(define (t x y) (lambda (m)(m x y)))
(define y (t 1 2))
(display (y +))
(define a (list 1 2 3 4 5))


(define (get items n)
  (if (= n 0)
      (car items)
      (get (cdr items) (- n 1))))

(define (len L)
  (if (null? L)
      0
      (+ 1 (len (cdr L)))))